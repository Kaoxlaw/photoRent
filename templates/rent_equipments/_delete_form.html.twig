<form method="post" action="{{ path('rent_equipments_delete', {'id': rent_equipment.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rent_equipment.id) }}">
    <button class="btn btn-outline-danger my-2 my-sm-0">Delete</button>
</form>
